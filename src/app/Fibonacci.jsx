import { Button, StyleSheet, View, Text } from "react-native";
import { router } from "expo-router";

const Fibonacci = () => {
    return (
        <View>
             <Text> Ingrese la posicion que quiere ver en la serie Fibonacci </Text> 
        </View> 
    )
}

export default Fibonacci;